version: '3.8'

services:
  app:
    build: .
    image: kommo-analytics
    container_name: kommo-analytics
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./config:/app/config
    restart: unless-stopped
